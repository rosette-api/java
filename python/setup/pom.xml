<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.basistech.ws</groupId>
        <artifactId>ws-client-bindings-python</artifactId>
        <version>0.1-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>ws-client-bindings-setup</artifactId>
    <properties>
        <mock-service-port>12345</mock-service-port>
	<!-- make raas-service-url blank to use above port of mock service -->
	<raas-service-url>http://jugmaster.basistech.net/rest/v1</raas-service-url>
    </properties>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.9</version>
                <executions>
                    <execution>
                        <id>unpack-data</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>com.basistech</groupId>
                                    <artifactId>common</artifactId>
                                    <version>${bt-common-version}</version>
                                    <outputDirectory>${project.build.directory}/autogen-data</outputDirectory>
                                    <includes>*.xml</includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>2.7</version>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${basedir}</outputDirectory>
                            <!-- can  we move this? -->
                            <resources>
                                <resource>
                                    <directory>src/setup</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.basistech.maven.plugins</groupId>
                <artifactId>autogen</artifactId>
                <version>5</version>
                <executions>
                    <execution>
                        <id>generate</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${basedir)/../../rosette</outputDirectory>
                            <language>py</language>
                            <templates>
                                <template>
                                    <code>src/main/templates/LanguageCode.py.tpl</code>
                                    <data>${project.build.directory}/autogen-data/bt_language_names.xml</data>
                                </template>
                                <template>
                                    <code>src/main/templates/ISO15924.py.tpl</code>
                                    <data>${project.build.directory}/autogen-data/iso15924-utf8.xml</data>
                                </template>
                            </templates>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>1.9.1</version>
                <executions>
                    <execution>
                        <id>reserve-network-port</id>
                        <goals>
                            <goal>reserve-network-port</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <portNames>
                                <portName>mock-service-port</portName>
                            </portNames>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.mock-server</groupId>
                <artifactId>mockserver-maven-plugin</artifactId>
                <version>3.6.2</version>
                <configuration>
                    <serverPort>${mock-service-port}</serverPort>
                    <logLevel>DEBUG</logLevel>
                    <initializationClass>com.basistech.webservices.mock.RaasMockServices</initializationClass>
                </configuration>
                <executions>
                    <execution>
                        <id>process-test-classes</id>
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>start</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>verify</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>stop</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>com.basistech.ws</groupId>
                        <artifactId>ws-bus-mock-web-service</artifactId>
                        <version>0.1.1</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <configuration>
                    <executable>bash</executable>
                    <workingDirectory>${basedir}</workingDirectory>
                    <environmentvariables>
                        <VERSION>${project.version}</VERSION>
                    </environmentvariables>
                </configuration>
                <executions>
                    <execution>
                        <id>setup-clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <arguments>
                                <argument>${run}</argument>
                                <argument>${virtualenv}</argument>
                                <argument>${python}</argument>
                                <argument>setup.py</argument>
                                <argument>clean</argument>
                            </arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>setup build</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <arguments>
                                <argument>${run}</argument>
                                <argument>${virtualenv}</argument>
                                <argument>${python}</argument>
                                <argument>setup.py</argument>
                                <argument>build</argument>
                            </arguments>
                        </configuration>
                    </execution>
<!-- This causes more problems than it solves so far,
        since it leaves droppings in the virtualenv we have nothing
        'downstream' to consume them.

                    <execution>
                        <id>setup install</id>
                        <phase>install</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <arguments>
                                <argument>${run}</argument>
                                <argument>${virtualenv}</argument>
                                <argument>${python}</argument>
                                <argument>setup.py</argument>
                                <argument>install</argument>
                            </arguments>
                        </configuration>
                    </execution>
-->
                    <!-- this isn't very compatible with mvn deploy until we have a repo or something.
                    <execution>
                        <id>setup deploy</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <arguments>
                                <argument>${run}</argument>
                                <argument>${virtualenv}</argument>
                                <argument>${python}</argument>
                                <argument>setup.py</argument>
                                <argument>register</argument>
                                <argument>sdist</argument>
                                <argument>upload</argument>
                            </arguments>
                        </configuration>
                    </execution>
-->
                    <execution>
                        <id>setuptools test</id>
                        <phase>test</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <skip>${maven.test.skip}</skip>
                            <arguments>
                                <argument>${run}</argument>
                                <argument>${virtualenv}</argument>
                                <argument>${python}</argument>
                                <argument>setup.py</argument>
                                <argument>nosetests</argument>
                            </arguments>
                            <environmentVariables>
                              <MOCK_SERVICE_PORT>${mock-service-port}</MOCK_SERVICE_PORT>
			      <RAAS_SERVICE_URL>${raas-service-url}</RAAS_SERVICE_URL>
                            </environmentVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
